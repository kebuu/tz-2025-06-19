Using DDD file structure, create a User model with the following fields :
- id: UUID
- pseudo: String
- email: String

##########################
Il en fait des tonnes, alors que j'ai pas demandé grand chose
Il crée tout du controller web à la BDD
##########################

import flyway as project dependancies and create a migration script to initialize a table named User and used to store the User model

Can you find the last version of flyway-core using web site https://mvnrepository.com/
=> Failed
##########################

The hard-coded URL in line 66 of UserController.kt was successfully replaced with MvcUriComponentsBuilder to dynamically build the location for the getUser endpoint. The implementation is error-free, and all tests passed successfully.
update the test of createUser to make sure the that the Uri contains in the response let to retrieve the newly created user

##########################

The server cannot start, can you fix the problem

##########################


